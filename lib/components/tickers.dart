import 'package:flutter/material.dart';

class TickerTab extends StatefulWidget {
  const TickerTab({Key? key}) : super(key: key);

  @override
  _TickerTabState createState() => _TickerTabState();
}

class _TickerTabState extends State<TickerTab> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('TickerTab'),
    );
  }
}
